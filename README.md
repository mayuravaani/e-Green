# e-Green
web based application for garbage disposal help center for public

To run
Start server
Insert the database(db_center.sql) in the server
Insert the files into appropriate folder(Eg: In XAMPP server htdocs and WAMP server www directory)
Start localhost and run the app
